name: go

on:
  push:
    branches:
      - master
      - develop
    tags:
      - v1
      - v1.*

jobs:
  build:
    name: build
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1

